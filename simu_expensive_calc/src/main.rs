use simu_expensive_calc::generate_workout;

fn main() {
    let simulate_user_intensity = 24;
    let simulate_random_number = 5;
    generate_workout(simulate_user_intensity, simulate_random_number);
}

